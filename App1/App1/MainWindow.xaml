<Window
    x:Class="App1.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:ws="using:Windows.Storage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Margin="12,0,12,0">
            <Button x:Name="RefreshCategoriesButton" Content="Refresh Categories" Margin="0,0,8,0" IsEnabled="False" />
            <Button x:Name="RefreshFilesButton" Content="Refresh File List" Margin="0,0,0,0" Click="RefreshFilesButton_Click" IsEnabled="False" />
        </StackPanel>
        <ListView x:Name="fileList" AllowDrop="False" Grid.Row="1" Grid.Column="0" Grid.RowSpan="2" MinWidth="150" MaxWidth="300" VerticalAlignment="Stretch" SelectionChanged="FileList_SelectionChanged" Margin="12,0,12,0" Background="{ThemeResource SystemControlAcrylicElementBrush}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="ws:StorageFile">
                    <Grid Margin="0,12,0,12" AutomationProperties.Name="{x:Bind DisplayName}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Image Source="{x:Bind Path}" Height="50" Width="50" />

                        <StackPanel Margin="12,0,0,0" Grid.Column="1" >
                            <TextBlock Text="{x:Bind DisplayName}" FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold" Style="{ThemeResource BaseTextBlockStyle}" HorizontalAlignment="Left" Margin="0,0,0,6" LineHeight="20"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1">
            <TextBox x:Name="filePathBox" Header="Base Path" MinWidth="450" Margin="0,0,0,8" />
            <AppBarButton Icon="OpenLocal" Label="Open" Click="PickFolderButton_Click">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control" Key="O" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
            <AppBarSeparator />
            <TextBox x:Name="filterTypesBox" Header="File Types" Text=".jpg,.png,.bmp,.gif"  MinWidth="300" Margin="0,0,0,8" />
            <AppBarButton Icon="Filter" Label="Filter" Click="FilterButton_Click" />
        </StackPanel>
        <Border Grid.Row="1" Grid.Column="1"
                                Background="{ThemeResource SystemControlAcrylicElementBrush}"
                                VerticalAlignment="Top" 
                                HorizontalAlignment="Left" 
                                Margin="16" 
                                CornerRadius="8"
                                BorderThickness="1">
            <Image x:Name="ImagePreview" Stretch="Uniform" />
        </Border>
        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" Grid.RowSpan="2" MaxHeight="100" VerticalAlignment="Top" Margin="12,12,0,0">
            <ComboBox x:Name="FolderList" MinWidth="200" MaxWidth="300" />
            <Button x:Name="MoveButton" Content="Move To Selected" Margin="8,0,4,0" Click="MoveButton_Click" IsEnabled="False" />
            <Button x:Name="NewCategoryButton" Content="Add Category" Margin="4,0,4,0">
                <Button.Flyout>
                    <Flyout>
                        <StackPanel Orientation="Horizontal">
                            <TextBox x:Name="NewCatName" Header="Category Name" MinWidth="300" MaxLength="255" TextChanged="NewCatName_TextChanged" />
                            <Button x:Name="NewCatAddButton" Content="OK" Margin="8,0,0,0" VerticalAlignment="Bottom" Click="NewCategoryButton_Click" IsEnabled="False" />
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
            <GridView x:Name="CategoryGrid"
                      Margin="4,0,4,0"
                      Background="{ThemeResource SystemControlAcrylicElementBrush}" 
                      HorizontalAlignment="Stretch" 
                      MinWidth="400"
                      SelectionMode="None"
                      IsItemClickEnabled="True"
                      CanDragItems="False"
                      AllowDrop="False"
                      FlowDirection="LeftToRight">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="ws:StorageFolder">
                        <StackPanel Width = '240' Orientation = 'Horizontal'>
                            <TextBlock Style = '{StaticResource BodyTextBlockStyle}' Margin = '8,0,0,0' Text = '{x:Bind DisplayName}'
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>

                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid x:Name="MaxItemsWrapGrid" 
                           MaximumRowsOrColumns="2" 
                           Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
            </GridView>
            <DropDownButton x:Name="ConvertButton" Content="Convert" IsEnabled="False">
                <DropDownButton.Flyout>
                    <MenuFlyout x:Name="ConvertMenu">
                        <MenuFlyoutItem Text="PNG" Tag="PNG" Click="ConvertOption_Click" />
                        <MenuFlyoutItem Text="JPEG" Tag="JPEG" Click="ConvertOption_Click" />
                        <MenuFlyoutItem Text="GIF" Tag="GIF" Click="ConvertOption_Click" />
                        <MenuFlyoutSeparator />
                        <MenuFlyoutItem Text="HEIF" Tag="HEIF" Click="ConvertOption_Click" />
                        <MenuFlyoutItem Text="JPEG-XR" Tag="JXR" Click="ConvertOption_Click" />
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
        </StackPanel>
    </Grid>
</Window>